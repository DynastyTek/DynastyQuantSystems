name: Validate Blueprint (Quant)

on:
  push:
    branches: [ main ]
    paths:
      - dynasty_quant_systems.json
      - .github/workflows/lovable-deploy.yml
  workflow_dispatch:

concurrency:
  group: validate-quant
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify blueprint exists
        run: test -f dynasty_quant_systems.json

      - name: Validate JSON (jq)
        run: |
          sudo apt-get update && sudo apt-get install -y jq
          jq . dynasty_quant_systems.json > /dev/null

      - name: Archive blueprint
        uses: actions/upload-artifact@v4
        with:
          name: quant-blueprint
          path: dynasty_quant_systems.json
